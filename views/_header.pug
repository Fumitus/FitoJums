.header.header 
    nav.nav.nav--tours 
        a.nav__el(href='/?page=1') Naujas įrašas
    .header__logo
        img(src='/img/FitoJums_transparent_3.png' alt="FitoJums logo")
    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout Atsijungti
            a.nav__el(href='/me')
                img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
                span= user.name.split(' ')[0]
        else
            a.nav__el(href='/login') Prisijungti 
            